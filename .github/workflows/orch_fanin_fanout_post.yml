name: Post Process

on:
  workflow_run:
    workflows: ["CI matrix builds + E2E (workflow A)"]
    types:
      - completed
    branches:
      - main

jobs:
  post:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Post-run step
        run: echo "Downstream workflow triggered after successful CI run"