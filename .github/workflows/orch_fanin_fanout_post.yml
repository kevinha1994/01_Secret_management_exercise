name: Post Process

on:
  workflow_run:
    workflows: ["orch_fanin_fanout.yml"]
    types:
      - completed
    branches:
      - main

permissions:
  actions: read
  contents: read

jobs:
  post:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Post-run step
        run: echo "Downstream workflow triggered after successful CI run"